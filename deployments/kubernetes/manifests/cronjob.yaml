---
# Source: k8scostoptimizer/templates/cronjob.yaml
apiVersion: batch/v1
kind: CronJob
metadata:
  labels:
    app: k8s-cost-optimizer-k8scostoptimizer
    chart: "k8scostoptimizer-v0.0.1-91404c2"
    release: "k8s-cost-optimizer"
    heritage: "Helm"
    app.kubernetes.io/managed-by: "Helm"
    group: com.stakater.platform
    provider: stakater
    version: v0.0.1-91404c2
  name: k8s-cost-optimizer-k8scostoptimizer
  namespace: default
spec:
  schedule: "*/2 * * * *"
  startingDeadlineSeconds: 200
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: k8s-cost-optimizer-k8scostoptimizer
            chart: "k8scostoptimizer-v0.0.1-91404c2"
            release: "k8s-cost-optimizer"
            heritage: "Helm"
            app.kubernetes.io/managed-by: "Helm"
            group: com.stakater.platform
            provider: stakater
            version: v0.0.1-91404c2
        spec:
          containers:
            - name: k8s-cost-optimizer-k8scostoptimizer
              image: "stakater/k8scostoptimizer:v0.0.1-91404c2"
              imagePullPolicy: IfNotPresent
              args:
              - "--config-file-path=/app/config.yaml"
              - "--patch=true"
              volumeMounts:
              - name: config
                mountPath: /app
                subPath: config.yaml
          restartPolicy: Never
          volumes:
          - name: config
            configMap:
              name: k8s-cost-optimizer-k8scostoptimizer-config
          terminationGracePeriodSeconds: 30
          dnsPolicy: ClusterFirst
          securityContext: 
            runAsNonRoot: true
            runAsUser: 65534
          serviceAccountName: k8s-cost-optimizer-k8scostoptimizer
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 2
